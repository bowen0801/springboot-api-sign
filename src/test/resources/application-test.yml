# 测试环境配置 - 关闭外部依赖，使用 Mock
api:
  sign-expire: 300
  use-database: false
  use-hmac: false
  enable-rate-limit: false          # 关闭限流，避免测试干扰
  rate-limit-count: 1000
  app:
    key: test_app_key_123456
    secret: test_app_secret_654321_abcdef123456

logging:
  level:
    root: WARN
    com.example.springbootapisign: DEBUG
